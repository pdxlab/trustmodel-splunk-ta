#
# Transforms — primarily reserved for future enrichment lookups (org name,
# model vendor, framework label) once we ship the lookup table CSVs.
#
# Field aliases live in props.conf for inline performance.
#

[trustmodel_severity_to_priority]
# Future: map TrustModel severity (low/medium/high/critical) to ES priority.
# Provided as a stub now so the lookup file path stays stable for upgrades.
filename = trustmodel_severity_priority.csv
case_sensitive_match = false
